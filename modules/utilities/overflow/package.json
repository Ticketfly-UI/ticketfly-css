{
  "name": "ticketfly-css-overflow",
  "version": "0.0.3",
  "description": "Overflow utilities for ticketfly-css",
  "main": "dist/ticketfly-css-overflow.css",
  "style": "dist/ticketfly-css-overflow.css",
  "scripts": {
    "clean": "del dist/**",
    "transform": "mkdir -p dist && postcss --config postcss.config.js",
    "minify": "postcss -u cssnano dist/ticketfly-css-overflow.css -o dist/ticketfly-css-overflow.min.css",
    "build": "npm run clean && npm run transform",
    "build:min": "npm run build && npm run minify",
    "prepublish": "mkdir -p dist && npm run build"
  },
  "repository": {
    "url": "git@github.com:Ticketfly/ticketfly-css.git",
    "type": "git"
  },
  "files": [
    "dist/ticketfly-css-overflow.css"
  ],
  "bugs": {
    "url": "https://github.com/Ticketfly/ticketfly-css/issues"
  },
  "homepage": "https://github.com/Ticketfly/ticketfly-css/issues",
  "author": "Brian Sipple <@Brian_Sipple>",
  "license": "MIT",
  "devDependencies": {
    "cssnano": "^3.9.1",
    "del-cli": "^0.2.1",
    "postcss-cli": "^2.6.0"
  }
}
